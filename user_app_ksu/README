#
# KernelSU manager APK (me.weishu.kernelsu).
#
# Packaged as prebuilt_etc into /system/etc/user_app/kernelsu.apk and installed
# with pm on first boot by init.sh. Skipped when BLISS_REMOVE_KSU=true (--noksu).
#
